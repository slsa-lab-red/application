apiVersion: apps/v1
kind: Deployment
metadata:
  name: slsa-simulation-app
  namespace: slsa-simulation
  labels:
    app: slsa-simulation-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: slsa-simulation-app
  template:
    metadata:
      labels:
        app: slsa-simulation-app
    spec:
      containers:
      - name: web
        image: ghcr.io/<ORG>/application:latest
        ports:
          - containerPort: 80
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        readinessProbe:
          httpGet:
            path: /
            port: 80